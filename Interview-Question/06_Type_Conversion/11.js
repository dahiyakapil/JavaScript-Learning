/*
    What is the return result when attempting to convert a null value to a number?

    In JavaScript, when attempting to convert a null value to a number using the Number() constructor, the result is 0.

*/

let value = null;

let res = Number(value);
console.log(res);
