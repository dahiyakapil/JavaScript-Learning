/*
    What is the result of an explicit attempt to convert a non-number to a number?

    The output will be NaN, when converting a non-number to a number.
*/

let str = Number("eirnirnifo infienfe");
console.log(str);
console.log(typeof str)
