/*
    What is type coercion in JavaScript?
   
    Type coercion in JavaScript refers to the automatic or implicit conversion of values from one data type to another during the execution of operations involving different types.
 */

let num = 123;
let str = "5";
let res = num + str; // Implicit(Automatic) type coersion
console.log(res);
