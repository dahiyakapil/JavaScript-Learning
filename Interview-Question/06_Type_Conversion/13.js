/*
    Can you give a brief description of the potential outcomes when converting a string to a number?

    In JavaScript, converting a string to a number can result in a valid number, NaN for non-numeric strings, or 0 for empty strings.

*/

let str = "";
let str2 = null;
let str3 = undefined;
let str4 = NaN;

console.log(Number(str));
console.log(Number(str2));
console.log(Number(str3));
console.log(Number(str4));
