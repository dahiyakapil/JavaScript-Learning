/*
    What is the valueOf() method in JavaScript?

    he valueOf() method is a built-in method in JavaScript that returns the primitive value of an object.
*/

let str = new String("Hello............");
let res = str.valueOf();
console.log(res);
