/*
    If there is an attempt to convert an undefined value to a number. What is the return result?

    The output will be NaN

*/

let value; // undifined in global scope...

let result = Number(value);
console.log(result);
