/*
    String methods used for accessing

    1. length
    2. charAt(index)
    3. charCodeAt(index)
    4. charPointAt(index)

*/
const str = "My name is Kapil 😊";
console.log(str.length);
console.log(str.charAt(3)); // space is also calculated
console.log(str.charCodeAt(1));// returns the Unicode of M
console.log(str.codePointAt(1));

